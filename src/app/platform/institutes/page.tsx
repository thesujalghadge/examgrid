import { PlaceholderPanel } from "@/components/workspace/placeholder-panel";

export default function PlatformInstitutesPage() {
  return (
    <PlaceholderPanel
      title="Institute Tenants"
      description="Manage institute onboarding, lifecycle, plan allocations, and tenant isolation controls."
    />
  );
}
