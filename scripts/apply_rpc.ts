import { Client } from "pg";
import fs from "fs";
import "dotenv/config";

async function main() {
  // Parse Supabase Postgres connection string
  // If we don't have it, we can't use pg easily. Let's just use powershell to move files.
}
main();
